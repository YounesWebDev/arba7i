const dictionary = {
  "title": "Orders",
  "description": "This page will centralize new orders, statuses, and fulfillment in a single workflow.",
  "backToDashboard": "Back to dashboard"
} as const;

export default dictionary;
