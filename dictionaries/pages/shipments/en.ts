const dictionary = {
  "title": "Shipments",
  "description": "This page will handle shipping operations, tracking statuses, and delivery details.",
  "backToDashboard": "Back to dashboard"
} as const;

export default dictionary;
