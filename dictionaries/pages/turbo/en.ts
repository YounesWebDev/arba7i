const dictionary = {
  "title": "Turbo",
  "description": "This section is reserved for quick actions and tools that speed up daily operations.",
  "backToDashboard": "Back to dashboard"
} as const;

export default dictionary;
