const dictionary = {
  "title": "Page not found",
  "description": "The page you requested does not exist yet or has not been built.",
  "backToHome": "Back to home",
  "backToDashboard": "Back to dashboard",
} as const;

export default dictionary;
